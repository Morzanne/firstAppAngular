import { TheWidthDirective } from './the-width.directive';

describe('TheWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new TheWidthDirective();
    expect(directive).toBeTruthy();
  });
});
